# python
venv/
*.tmp.*

# === Build output (CMake) ===
build/
build-*/
out/
cmake-build-*/

# CMake cache & generated files (nếu lỡ build ngay trong source dir)
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
CTestTestfile.cmake
Makefile
compile_commands.json
*.cmake
!CMakeLists.txt

# === Compiled objects / binaries ===
*.o
*.obj
*.a
*.lib
*.so
*.so.*
*.dylib
*.dll
*.exe
*.out

# === Model files (nặng, không nên commit vào git) ===
*.gguf
*.safetensors
*.bin
models/

# === IDE / Editor ===
.vscode/
.idea/
*.swp
*.swo

# === Debug / profiling ===
*.pdb
*.dSYM/
compile_commands.json
.cache/          # ccache / clangd cache

# === OS junk ===
.DS_Store
Thumbs.db

# === Log / runtime data ===
*.log
*.pid

# === Ghi chú riêng cho submodule llama.cpp ===
# KHÔNG ignore thư mục submodule (third_party/llama.cpp) — git submodule tự quản lý,
# nhưng phải ignore build output của chính llama.cpp nếu build lồng trong đó
vendor/llama.cpp/build/