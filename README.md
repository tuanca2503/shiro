# shiro
A self-hosted LLM engine with an agentic tool-calling loop — runs a local model as a persistent server and lets it reason about when to call external tools to answer questions it can't from its own weights.
