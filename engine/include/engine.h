// engine/include/engine.h — ĐÂY là "hợp đồng" ổn định, agent chỉ biết đến file này
class Engine {
public:
    // virtual bool loadModel(const std::string& path) = 0;
    // virtual std::string generate(const std::string& prompt) = 0;
    // virtual std::string generateStream(const std::string& prompt, StreamCallback cb) = 0;
};